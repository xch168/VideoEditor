<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.m4399.videoeditor"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.VideoListActivity">
        </activity>
        <activity android:name=".ui.VideoClipActivity">
        </activity>
        <activity android:name=".ui.VideoPreviewActivity">
        </activity>
        <activity android:name=".ui.EditThumbActivity">
        </activity>

        <activity android:name=".ui.EditThumb2Activity">
        </activity>
    </application>

</manifest>